<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Main application theme, Material 3 -->
    <style name="Theme.HerbalEncyclopedia" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/brand_primary</item>
        <item name="android:navigationBarColor">@color/brand_background</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/brand_background</item>
    </style>

    <!-- تنسيق مربعات حوار JS (alert/confirm/prompt) الصادرة من WebView لتبدو
         كجزء من التطبيق بدل مربع Android القديم الذي يعرض عنوان الصفحة -->
    <style name="HerbalAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="shapeAppearanceOverlay">@style/HerbalAlertDialog.Shape</item>
        <item name="buttonBarPositiveButtonStyle">@style/HerbalAlertDialog.Button</item>
        <item name="buttonBarNegativeButtonStyle">@style/HerbalAlertDialog.Button</item>
    </style>

    <style name="HerbalAlertDialog.Shape" parent="">
        <item name="cornerSize">20dp</item>
    </style>

    <style name="HerbalAlertDialog.Button" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">@color/brand_primary</item>
    </style>

    <!-- Splash screen theme shown while the WebView content loads -->
    <style name="Theme.HerbalEncyclopedia.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_primary</item>
        <item name="postSplashScreenTheme">@style/Theme.HerbalEncyclopedia</item>
    </style>

</resources>
